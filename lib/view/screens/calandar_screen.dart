import 'package:flutter/material.dart';

class CalandarScreen extends StatelessWidget {
  const CalandarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("calander")),
    );
  }
}